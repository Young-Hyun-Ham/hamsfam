# Admin API (관리자)
이 폴더는 사용자용 Admin 도메인 API Endpoint를 관리합니다.  
DB 백엔드는 설정에 따라 `firebase` / `postgres` 버전으로 분리되어 있습니다.

> ✅ firebase: 운영 기준(완료)
> 🧪 postgres: 초기 구현(임시), 버전업 예정

---