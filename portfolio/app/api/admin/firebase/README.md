# Admin API (Firebase)

이 디렉토리는 **관리자(Admin) 전용 API Endpoint**를 관리합니다.  
모든 API는 **Firebase(Firestore) 기반**으로 구현되어 있으며,  
관리자 권한이 필요한 기능만 포함합니다.

> 📌 사용자 API와 분리된 **Admin 전용 영역**  
> 📌 관리자 대시보드 Front(`app/(sidebar-header)/admin`)와 매핑

---

## 📂 패키지 구조

```bash
app/api/admin/firebase/
├── board/                   # 관리자 게시판 관리
├── knowledge/               # 지식 프로젝트 / 인텐트 / 엔티티 관리
├── menu/                    # 관리자 메뉴 관리
├── shortcut-menu/           # 바로가기 메뉴 관리
├── stats/                   # 통계(대시보드) 데이터
├── token-manage/            # 토큰 충전 / 차감 / 이력 관리
├── train/                   # 학습 / 임베딩 / 트레이닝 작업
├── user-info/               # 관리자용 사용자 정보 관리
└── user-stats/              # 사용자 활동 통계
```